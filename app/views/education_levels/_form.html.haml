= form_for @education_level do |f|
  - if @education_level.errors.any?
    #error_explanation
      %h2= "#{pluralize(@education_level.errors.count, "error")} prohibited this education_level from being saved:"
      %ul
        - @education_level.errors.full_messages.each do |msg|
          %li= msg

  .field
    = f.label :name
    = f.text_field :name
  .actions
    = f.submit 'Save'
