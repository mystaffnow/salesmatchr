= form_for @sales_type do |f|
  - if @sales_type.errors.any?
    #error_explanation
      %h2= "#{pluralize(@sales_type.errors.count, "error")} prohibited this sales_type from being saved:"
      %ul
        - @sales_type.errors.full_messages.each do |msg|
          %li= msg

  .field
    = f.label :name
    = f.text_field :name
  .actions
    = f.submit 'Save'
